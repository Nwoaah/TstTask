Решил сделать отрицательные id ошибкой, для этого сдела эксепшен, почти все ошибки решил сделать через illegalargumentException, сократить себе время на придумывание названий на каждый всевозможный тест.
Реализации метода 2, одна работает с json через его элементы(Method.value) а вторая десереализует его в java объекты(MyObj.value).
Поведение методов немного отличается, в первой, где Method идет парсинг jsona через его элементы, отсюда может падать в ошибку, если в json приходят в качестве значений полей null,
во второй же MyObj, падает с тольо если status : null, это не было оговорено, поэтому у обоих методов оставил их поведение таким, какое получилось.
В MyObj так же получается, что при нарушении контракта, например sum вместо amount, значению amount присваивается null, а он, как уже ранее описано - проходит.
UPDATE:
так как последнее замеченное поведение неприемлимо,
в классе MyObj заменил примитивный тип int на враппер Integer, достигнуто нужное поведение.

Тесты на обеих реализациях одни и те же(кроме одного самого первого в MyObj), поведение отличается только в выбрасываемых исключениях.
Так же в MyObj.main небольшой тест на производительность, там есть генератор, который создает большой json и подает его двум методам на вход, вычисляя время выполнения, при большом количестве данных, время выполнения
метода, в котором работа происходит с java объектами(MyObj.value) скорость выполнения на порядок выше(под порядком подразумеваю 1 значащую цифру)